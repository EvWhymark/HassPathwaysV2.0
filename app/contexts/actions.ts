// app/context/actions.ts

// Action type for initializing data load
export const INITIAL_LOAD_DATA = "initial_load_data";

// Action type for setting the catalog year
export const SET_CATALOG = "set_catalog";
